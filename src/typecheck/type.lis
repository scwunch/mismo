import "mode"

pub enum Type {
  Never,
  Nil,
  Bool,
  Nat,
  Int,
  Float,
  String,
  Tuple(Slice<Type>),
  Fn(Slice<ParamType>, Ref<Type>),
  Adt(Ref<TypeDef>, Slice<Type>),
}

pub struct ParamType {
  pub mode: mode.Mode,
  pub typ: Type,
}
