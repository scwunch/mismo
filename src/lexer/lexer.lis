

pub fn lex(_source: string) -> Slice<Token> {
  let tokens: Slice<Token> = []
  tokens
}

pub struct Lexer {
  pub source: string,
  pub tokens: Slice<Token>,
}

impl Lexer {
  pub fn new(source: string) -> Lexer {
    Lexer { source: source, tokens: [] }
  }
}