import "specialization"

pub type Registry = specialization.ConcreteRegistry

pub fn generate(reg: Registry) -> string {
  let mut emitter = Emitter.new(reg)
  emitter.emit_program()
  emitter.out()
}
